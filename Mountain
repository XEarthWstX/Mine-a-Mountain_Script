-- [[ Daley Hub - Kavo Exploration & Utility Suite v2.4.0 (Fixed & Runes Included) ]] --

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Workspace = game:GetService("Workspace")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local LP = Players.LocalPlayer
local Camera = Workspace.CurrentCamera

-- ==========================================
-- [[ REMOTE & INSTANCE PATHS (SAFE SCAN) ]] --
-- ==========================================
local RemotesFolder = ReplicatedStorage:WaitForChild("Remotes", 3)
local DigRequest = RemotesFolder and RemotesFolder:WaitForChild("DigRequest", 2)
local SellRequest = RemotesFolder and RemotesFolder:WaitForChild("SellRequest", 2)
local UpgradeBuy = RemotesFolder and RemotesFolder:WaitForChild("UpgradeBuy", 2) 

local ThingsFolder = Workspace:WaitForChild("Things", 3)
local CrystalsFolder = ThingsFolder and ThingsFolder:WaitForChild("Crystals", 2) or Workspace:FindFirstChild("Crystals")

-- ค้นหาโฟลเดอร์ Runes แบบปลอดภัย ป้องกันสคริปต์ค้าง (Non-blocking)
local Mountain = Workspace:FindFirstChild("MountainDecoration")
local RunesFolder = (ThingsFolder and ThingsFolder:FindFirstChild("Runes")) 
                 or Workspace:FindFirstChild("Runes") 
                 or (Mountain and Mountain:FindFirstChild("Runes"))

-- ดักจับการโหลดโฟลเดอร์ในแมพย้อนหลัง
if ThingsFolder then
    ThingsFolder.ChildAdded:Connect(function(child)
        if child.Name == "Crystals" then CrystalsFolder = child
        elseif child.Name == "Runes" then RunesFolder = child end
    end)
end
Workspace.ChildAdded:Connect(function(child)
    if child.Name == "Runes" then RunesFolder = child
    elseif child.Name == "MountainDecoration" then
        Mountain = child
        local r = child:FindFirstChild("Runes")
        if r then RunesFolder = r end
    end
end)

-- ==========================================
-- [[ STATE STORAGE ]] --
-- ==========================================
local States = {
    -- Locomotion
    SpeedEnabled  = false,
    SpeedValue    = 50,
    FlyEnabled    = false,
    FlySpeed      = 50,
    NoclipEnabled = false,

    -- Visuals / Scanner
    PlayerESP      = false,
    CrystalESP     = false,
    RuneESP        = false,
    SelectedPlayer = nil,

    -- Coordinates Storage
    InputCoords    = "0, 0, 0",

    -- Automation Suite
    AutoDigEnabled   = false,
    DiggerToolName   = "Rusty Scrapper",
    AutoSellEnabled  = false,
    
    -- Air Upgrades
    AutoAir1      = false,
    AutoAir2      = false,
    AutoAir3      = false,

    -- Weight Upgrades
    AutoWeight1      = false,
    AutoWeight2      = false,
    AutoWeight3      = false,

    -- Crystal Farming
    AutoCrystal      = false,
    CurrentCrystal   = nil,
    CrystalHoldTime  = 5,        
    LockTimestamp    = 0,
    SkipCurrent      = false,

    -- Rune Farming
    AutoRune         = false,
    CurrentRune      = nil
}

local Player_ESP_Cache = {}
local Crystal_ESP_Cache = {}
local Rune_ESP_Cache = {}
local PermanentBlacklist = {}  
local CrystalTargetStartTime = 0 

-- ==========================================
-- [[ UTILITY FUNCTIONS ]] --
-- ==========================================
local function GetRoot(player)
    local p = player or LP
    local char = p.Character
    return char and (char:FindFirstChild("HumanoidRootPart") or char:FindFirstChild("Torso") or char:FindFirstChildWhichIsA("BasePart"))
end

local function GetHumanoid(player)
    local p = player or LP
    local char = p.Character
    return char and char:FindFirstChildOfClass("Humanoid")
end

local function GetOtherPlayers()
    local names = {}
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= LP then table.insert(names, p.Name) end
    end
    if #names == 0 then table.insert(names, "None") end
    return names
end

local function GetNearestCrystal()
    if not CrystalsFolder then return nil end
    local myRoot = GetRoot()
    if not myRoot then return nil end

    local closest = nil
    local shortestDistance = math.huge

    for _, crystal in ipairs(CrystalsFolder:GetChildren()) do
        if crystal and crystal.Parent and not PermanentBlacklist[crystal] then
            local targetPart = crystal:IsA("BasePart") and crystal or crystal:FindFirstChildWhichIsA("BasePart", true)
            if targetPart then
                local dist = (myRoot.Position - targetPart.Position).Magnitude
                if dist < shortestDistance then
                    shortestDistance = dist
                    closest = crystal
                end
            end
        end
    end
    return closest
end

local function GetNearestRune()
    local folder = RunesFolder or (ThingsFolder and ThingsFolder:FindFirstChild("Runes")) or Workspace:FindFirstChild("Runes")
    if not folder and Mountain then folder = Mountain:FindFirstChild("Runes") end
    if not folder then return nil end

    local myRoot = GetRoot()
    if not myRoot then return nil end

    local closest = nil
    local shortestDistance = math.huge

    for _, rune in ipairs(folder:GetChildren()) do
        if rune and rune.Parent then
            local targetPart = rune:IsA("BasePart") and rune or rune:FindFirstChildWhichIsA("BasePart", true)
            if targetPart then
                local dist = (myRoot.Position - targetPart.Position).Magnitude
                if dist < shortestDistance then
                    shortestDistance = dist
                    closest = rune
                end
            end
        end
    end
    return closest
end

-- ==========================================
-- [[ ESP SYSTEMS ]] --
-- ==========================================

-- Player Chams
local function CreatePlayerESP(player)
    if player == LP or Player_ESP_Cache[player] then return end
    local container = {}

    local function applyESP(char)
        if not char then return end
        if container.Highlight then pcall(function() container.Highlight:Destroy() end) end

        local hl = Instance.new("Highlight")
        hl.Name = "DaleyKavo_PlayerHighlight"
        hl.FillColor = Color3.fromRGB(0, 255, 150)
        hl.FillTransparency = 0.5
        hl.OutlineColor = Color3.fromRGB(255, 255, 255)
        hl.Adornee = char
        hl.Enabled = States.PlayerESP
        hl.Parent = char
        container.Highlight = hl
    end

    if player.Character then applyESP(player.Character) end
    player.CharacterAdded:Connect(applyESP)
    Player_ESP_Cache[player] = container
end

local function RemovePlayerESP(player)
    if Player_ESP_Cache[player] then
        pcall(function()
            if Player_ESP_Cache[player].Highlight then Player_ESP_Cache[player].Highlight:Destroy() end
        end)
        Player_ESP_Cache[player] = nil
    end
end

-- Crystal Chams
local function CreateCrystalESP(crystal)
    if Crystal_ESP_Cache[crystal] then return end
    
    local hl = Instance.new("Highlight")
    hl.Name = "DaleyKavo_CrystalHighlight"
    hl.FillColor = Color3.fromRGB(190, 0, 255)
    hl.FillTransparency = 0.4
    hl.OutlineColor = Color3.fromRGB(255, 255, 255)
    hl.Adornee = crystal
    hl.Enabled = States.CrystalESP
    hl.Parent = crystal
    
    Crystal_ESP_Cache[crystal] = hl
end

local function RemoveCrystalESP(crystal)
    if Crystal_ESP_Cache[crystal] then
        pcall(function()
            Crystal_ESP_Cache[crystal]:Destroy()
        end)
        Crystal_ESP_Cache[crystal] = nil
    end
end

-- Rune Chams
local function CreateRuneESP(rune)
    if Rune_ESP_Cache[rune] then return end
    
    local hl = Instance.new("Highlight")
    hl.Name = "DaleyKavo_RuneHighlight"
    hl.FillColor = Color3.fromRGB(0, 230, 255)
    hl.FillTransparency = 0.3
    hl.OutlineColor = Color3.fromRGB(255, 255, 255)
    hl.OutlineTransparency = 0
    hl.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
    hl.Adornee = rune
    hl.Enabled = States.RuneESP
    hl.Parent = rune
    
    Rune_ESP_Cache[rune] = hl
end

local function RemoveRuneESP(rune)
    if Rune_ESP_Cache[rune] then
        pcall(function()
            Rune_ESP_Cache[rune]:Destroy()
        end)
        Rune_ESP_Cache[rune] = nil
    end
end

-- Initialize ESPs
for _, p in ipairs(Players:GetPlayers()) do CreatePlayerESP(p) end
Players.PlayerAdded:Connect(CreatePlayerESP)
Players.PlayerRemoving:Connect(RemovePlayerESP)

if CrystalsFolder then
    for _, crystal in ipairs(CrystalsFolder:GetChildren()) do
        CreateCrystalESP(crystal)
    end
    CrystalsFolder.ChildAdded:Connect(CreateCrystalESP)
    CrystalsFolder.ChildRemoved:Connect(RemoveCrystalESP)
end

task.spawn(function()
    while true do
        local folder = RunesFolder or (ThingsFolder and ThingsFolder:FindFirstChild("Runes")) or Workspace:FindFirstChild("Runes")
        if not folder and Mountain then folder = Mountain:FindFirstChild("Runes") end

        if folder then
            for _, rune in ipairs(folder:GetChildren()) do
                CreateRuneESP(rune)
            end
            folder.ChildAdded:Connect(CreateRuneESP)
            folder.ChildRemoved:Connect(RemoveRuneESP)
            break
        end
        task.wait(1)
    end
end)

-- ==========================================
-- [[ KAVO LIBRARY CONFIGURATION ]] --
-- ==========================================
local Kavo = loadstring(game:HttpGet("https://raw.githubusercontent.com/xHeptc/Kavo-UI-Library/main/source.lua"))()
local Window = Kavo.CreateLib("Daley Hub - Utility Engine", "Midnight")

local MoveTab   = Window:NewTab("Movement")
local AutoTab   = Window:NewTab("Automation")
local RuneTab   = Window:NewTab("รูน")
local ScanTab   = Window:NewTab("Visuals & Scanner")
local CoordTab  = Window:NewTab("Coordinates Manager")

-- ==========================================
-- [[ MOVEMENT OPERATIONS ]] --
-- ==========================================
local MoveSec = MoveTab:NewSection("Locomotion Controls")

MoveSec:NewToggle("CFrame Speed Mode", "Speeds up horizontal translation vectors", function(v)
    States.SpeedEnabled = v
end)

MoveSec:NewSlider("Walk Speed Scale", "Adjust speed magnitude", 250, 16, function(v)
    States.SpeedValue = v
end)

MoveSec:NewToggle("Fly Mode", "Allows free floating across workspace axises", function(v)
    States.FlyEnabled = v
    local hum = GetHumanoid()
    if not v and hum then hum.PlatformStand = false end
end)

MoveSec:NewSlider("Fly Speed Scale", "Adjust fly movement vector magnitude", 250, 10, function(v)
    States.FlySpeed = v
end)

MoveSec:NewToggle("Noclip Mesh Overrides", "Wipes collisions for character limbs", function(v)
    States.NoclipEnabled = v
end)

-- ==========================================
-- [[ AUTOMATION OPERATIONS ]] --
-- ==========================================
local AutoSec = AutoTab:NewSection("Utility Automation Loops")

AutoSec:NewTextBox("Equipped Digger Tool Name", "Type your tool name (e.g. Rusty Scrapper)", function(text)
    States.DiggerToolName = text
end)

AutoSec:NewToggle("Auto Dig Toggle", "Passively fires digging remotes in background", function(v)
    States.AutoDigEnabled = v
end)

AutoSec:NewToggle("Auto Sell All Toggle", "Passively dumps full inventory to shop remote", function(v)
    States.AutoSellEnabled = v
end)

-- Crystal Farming Section
local CrystalSec = AutoTab:NewSection("Crystal Farming")

CrystalSec:NewToggle("Auto Farm Crystals", "Teleports to nearest crystal and automatically mines it", function(v)
    States.AutoCrystal = v
    if not v then 
        States.CurrentCrystal = nil 
    end
end)

CrystalSec:NewSlider("Crystal Hold Timer (s)", "Adjust how many seconds to stay at each crystal (0 = stay until broken)", 15, 0, function(v)
    States.CrystalHoldTime = v
end)

CrystalSec:NewButton("Next Crystal", "Skips the current targeted crystal instantly", function()
    if States.CurrentCrystal then
        States.SkipCurrent = true
    end
end)

CrystalSec:NewButton("Blacklist Crystal", "Permanently blocks this targeted crystal for the session", function()
    if States.CurrentCrystal then
        local targetToBlacklist = States.CurrentCrystal
        PermanentBlacklist[targetToBlacklist] = true
        States.CurrentCrystal = nil 
        Kavo:Notify("Blacklisted", "This crystal will no longer be targeted.", 3)
    else
        Kavo:Notify("Error", "No active crystal target to blacklist!", 3)
    end
end)

-- Upgrades Interface
local UpgradeSec = AutoTab:NewSection("Stat Upgrades")

UpgradeSec:NewToggle("Auto Buy Air Tier 1", "Repeatedly purchases Air Level 1", function(v)
    States.AutoAir1 = v
end)

UpgradeSec:NewToggle("Auto Buy Air Tier 2", "Repeatedly purchases Air Level 2", function(v)
    States.AutoAir2 = v
end)

UpgradeSec:NewToggle("Auto Buy Air Tier 3", "Repeatedly purchases Air Level 3", function(v)
    States.AutoAir3 = v
end)

UpgradeSec:NewToggle("Auto Buy Weight Tier 1", "Repeatedly purchases Weight Level 1", function(v)
    States.AutoWeight1 = v
end)

UpgradeSec:NewToggle("Auto Buy Weight Tier 2", "Repeatedly purchases Weight Level 2", function(v)
    States.AutoWeight2 = v
end)

UpgradeSec:NewToggle("Auto Buy Weight Tier 3", "Repeatedly purchases Weight Level 3", function(v)
    States.AutoWeight3 = v
end)

-- ==========================================
-- [[ RUNE OPERATIONS (หมวดหมู่ รูน) ]] --
-- ==========================================
local RuneSec = RuneTab:NewSection("ระบบค้นหา & ฟาร์ม Rune")

RuneSec:NewToggle("เปิด ESP มอง Rune (Rune ESP)", "แสดงกรอบไฮไลต์สีฟ้าใส Rune ทั้งหมดในแมพ", function(v)
    States.RuneESP = v
    for _, hl in pairs(Rune_ESP_Cache) do
        if hl then hl.Enabled = v end
    end
end)

RuneSec:NewToggle("ออโต้ฟาร์ม Rune (Auto Rune)", "วาร์ปไปหา Rune ที่ใกล้ที่สุดและทำการกดเก็บอัตโนมัติ", function(v)
    States.AutoRune = v
    if not v then
        States.CurrentRune = nil
    end
end)

RuneSec:NewButton("วาร์ปไป Rune ที่ใกล้ที่สุด", "เทเลพอร์ตตัวเราไปหา Rune ชิ้นที่ใกล้ที่สุดทันที", function()
    local rune = GetNearestRune()
    local root = GetRoot()
    if rune and root then
        local part = rune:IsA("BasePart") and rune or rune:FindFirstChildWhichIsA("BasePart", true)
        if part then
            root.CFrame = CFrame.new(part.Position + Vector3.new(0, 2, 0))
            Kavo:Notify("วาร์ปสำเร็จ", "เทเลพอร์ตไปหา Rune เรียบร้อยแล้ว", 3)
        end
    else
        Kavo:Notify("ข้อผิดพลาด", "ไม่พบ Rune ในบริเวณนี้!", 3)
    end
end)

-- ==========================================
-- [[ VISUALS & SCANNER OPERATIONS ]] --
-- ==========================================
local ScanSec = ScanTab:NewSection("Render Profiles & ESP")

ScanSec:NewToggle("Enable Server Chams ESP", "Toggles highlighting on all remote clients", function(v)
    States.PlayerESP = v
    for _, container in pairs(Player_ESP_Cache) do
        if container.Highlight then
            container.Highlight.Enabled = v
        end
    end
end)

ScanSec:NewToggle("Enable Crystal ESP", "Toggles full colored chams across all crystal assets", function(v)
    States.CrystalESP = v
    for _, highlight in pairs(Crystal_ESP_Cache) do
        if highlight then
            highlight.Enabled = v
        end
    end
end)

local PlayerDropdown = ScanSec:NewDropdown("Select Target Client", "Choose a player to execute actions on", GetOtherPlayers(), function(selected)
    States.SelectedPlayer = selected
end)

ScanSec:NewButton("Refresh Server Directory", "Rescans players in server instance", function()
    PlayerDropdown:Refresh(GetOtherPlayers())
end)

ScanSec:NewButton("Teleport to Target", "Instantly snaps your coordinate matrix to target player", function()
    if States.SelectedPlayer and States.SelectedPlayer ~= "None" then
        local targetPlr = Players:FindFirstChild(States.SelectedPlayer)
        local myRoot = GetRoot()
        local tRoot = GetRoot(targetPlr)
        if myRoot and tRoot then
            myRoot.CFrame = tRoot.CFrame * CFrame.new(0, 0, 3)
        end
    end
end)

-- ==========================================
-- [[ COORDINATE MANAGEMENT OPERATIONS ]] --
-- ==========================================
local CoordSec = CoordTab:NewSection("Waypoints & Vectors")

local CurrentPosLabel = CoordSec:NewLabel("Current Location: X:0, Y:0, Z:0")

CoordSec:NewButton("Teleport to Spawn", "Instantly warp back to spawn zone", function()
    local root = GetRoot()
    if root then
        root.CFrame = CFrame.new(Vector3.new(-13.78, 29.22, 1075.75))
    end
end)

CoordSec:NewButton("Copy Current Coordinates", "Saves your position straight to clipboard", function()
    local root = GetRoot()
    if root then
        local pos = root.Position
        local cleanCoords = string.format("%.2f, %.2f, %.2f", pos.X, pos.Y, pos.Z)
        
        if setclipboard then
            setclipboard(cleanCoords)
            Kavo:Notify("Success", "Coordinates copied to clipboard: " .. cleanCoords, 3)
        else
            Kavo:Notify("Executor Error", "Your executor does not support setclipboard!", 3)
        end
    end
end)

-- ==========================================
-- [[ DAEMON FRAME TRACKING LOOP CONNECTIONS ]] --
-- ==========================================

-- Proximity Prompt Trigger Daemon (Crystals & Runes)
task.spawn(function()
    while true do
        if fireproximityprompt then
            pcall(function()
                local target = nil
                if States.AutoCrystal and States.CurrentCrystal then
                    target = States.CurrentCrystal
                elseif States.AutoRune and States.CurrentRune then
                    target = States.CurrentRune
                end

                if target then
                    local prompt = target:FindFirstChildOfClass("ProximityPrompt") or target:FindFirstChildWhichIsA("ProximityPrompt", true)
                    if prompt then
                        fireproximityprompt(prompt)
                    end
                end
            end)
        end
        task.wait(0.1) 
    end
end)

-- Crystal Sequential Target Tracking Loop
task.spawn(function()
    while true do
        if States.AutoCrystal then
            local myRoot = GetRoot()
            if myRoot then
                local now = os.clock()
                
                local targetValid = States.CurrentCrystal and States.CurrentCrystal.Parent and States.CurrentCrystal:IsDescendantOf(Workspace)
                
                local forcedSkip = States.SkipCurrent
                if forcedSkip then
                    States.SkipCurrent = false
                end

                local timeIsUp = false
                if States.CrystalHoldTime > 0 then
                    timeIsUp = (now - States.LockTimestamp) >= States.CrystalHoldTime
                end
                
                if targetValid and States.CrystalHoldTime > 0 and (now - CrystalTargetStartTime > States.CrystalHoldTime + 10) then
                    PermanentBlacklist[States.CurrentCrystal] = true
                    States.CurrentCrystal = nil
                    targetValid = false
                end

                if not targetValid and not forcedSkip then
                    States.CurrentCrystal = nil
                    local newTarget = GetNearestCrystal()
                    if newTarget then
                        States.CurrentCrystal = newTarget
                        CrystalTargetStartTime = now
                        States.LockTimestamp = now
                    end
                elseif timeIsUp or forcedSkip then
                    local oldCrystal = States.CurrentCrystal
                    States.CurrentCrystal = nil
                    
                    local closest = nil
                    local shortestDistance = math.huge
                    
                    if CrystalsFolder then
                        for _, crystal in ipairs(CrystalsFolder:GetChildren()) do
                            if crystal and crystal.Parent and crystal ~= oldCrystal and not PermanentBlacklist[crystal] then
                                local targetPart = crystal:IsA("BasePart") and crystal or crystal:FindFirstChildWhichIsA("BasePart", true)
                                if targetPart then
                                    local dist = (myRoot.Position - targetPart.Position).Magnitude
                                    if dist < shortestDistance then
                                        shortestDistance = dist
                                        closest = crystal
                                   end
                                end
                            end
                        end
                    end
                    
                    if closest then
                        States.CurrentCrystal = closest
                        CrystalTargetStartTime = now
                        States.LockTimestamp = now
                    else
                        States.CurrentCrystal = oldCrystal
                        States.LockTimestamp = now 
                    end
                end

                if States.CurrentCrystal then
                    pcall(function()
                        local crystal = States.CurrentCrystal
                        local targetPart = crystal:IsA("BasePart") and crystal or crystal:FindFirstChildWhichIsA("BasePart", true)
                        if targetPart then
                            myRoot.Velocity = Vector3.new(0, 0, 0)
                            myRoot.RotVelocity = Vector3.new(0, 0, 0)
                            myRoot.CFrame = CFrame.new(targetPart.Position + Vector3.new(0, 1.5, 0))
                        end
                    end)
                end
            end
        end
        task.wait(0.01)
    end
end)

-- Rune Target Tracking & CFrame Loop
task.spawn(function()
    while true do
        if States.AutoRune then
            local myRoot = GetRoot()
            if myRoot then
                local rValid = States.CurrentRune and States.CurrentRune.Parent and States.CurrentRune:IsDescendantOf(Workspace)
                if not rValid then
                    States.CurrentRune = GetNearestRune()
                end

                if States.CurrentRune then
                    pcall(function()
                        local r = States.CurrentRune
                        local targetPart = r:IsA("BasePart") and r or r:FindFirstChildWhichIsA("BasePart", true)
                        if targetPart then
                            myRoot.Velocity = Vector3.new(0, 0, 0)
                            myRoot.RotVelocity = Vector3.new(0, 0, 0)
                            myRoot.CFrame = CFrame.new(targetPart.Position + Vector3.new(0, 2.0, 0))
                        end
                    end)
                end
            end
        end
        task.wait(0.05)
    end
end)

-- Remote Event Auto Dig Thread
task.spawn(function()
    while true do
        if States.AutoDigEnabled and DigRequest then
            pcall(function()
                DigRequest:FireServer(States.DiggerToolName, nil)
            end)
        end
        task.wait(0.1)
    end
end)

-- Remote Event Auto Sell Thread
task.spawn(function()
    while true do
        if States.AutoSellEnabled and SellRequest then
            pcall(function()
                SellRequest:FireServer("all")
            end)
        end
        task.wait(0.5)
    end
end)

-- Stat Upgrade Daemon Thread
task.spawn(function()
    while true do
        if UpgradeBuy then
            if States.AutoAir1 then pcall(function() UpgradeBuy:FireServer("Air", 1) end) end
            if States.AutoAir2 then pcall(function() UpgradeBuy:FireServer("Air", 2) end) end
            if States.AutoAir3 then pcall(function() UpgradeBuy:FireServer("Air", 3) end) end
            
            if States.AutoWeight1 then pcall(function() UpgradeBuy:FireServer("Weight", 1) end) end
            if States.AutoWeight2 then pcall(function() UpgradeBuy:FireServer("Weight", 2) end) end
            if States.AutoWeight3 then pcall(function() UpgradeBuy:FireServer("Weight", 3) end) end
        end
        task.wait(0.3) 
    end
end)

-- Positional Tracker Frame UI update loop
task.spawn(function()
    while true do
        local root = GetRoot()
        if root then
            local pos = root.Position
            CurrentPosLabel:UpdateLabel(string.format("Current Location: X: %.1f, Y: %.1f, Z: %.1f", pos.X, pos.Y, pos.Z))
        end
        task.wait(0.2)
    end
end)

-- Core Physics & Locomotion Thread Hook
RunService.Heartbeat:Connect(function(deltaTime)
    local root = GetRoot()
    local hum = GetHumanoid()
    if not root or not hum then return end

    -- Noclip Loop
    if States.NoclipEnabled and LP.Character then
        for _, descendant in ipairs(LP.Character:GetChildren()) do
            if descendant:IsA("BasePart") then descendant.CanCollide = false end
        end
    end

    -- Fly Loop
    if States.FlyEnabled then
        hum.PlatformStand = true
        local flyDirection = Vector3.new(0, 0, 0)
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then flyDirection = flyDirection + Camera.CFrame.LookVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then flyDirection = flyDirection - Camera.CFrame.LookVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then flyDirection = flyDirection - Camera.CFrame.RightVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then flyDirection = flyDirection + Camera.CFrame.RightVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then flyDirection = flyDirection + Vector3.new(0, 1, 0) end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftShift) then flyDirection = flyDirection - Vector3.new(0, 1, 0) end
        if flyDirection.Magnitude > 0 then root.Velocity = flyDirection.Unit * States.FlySpeed else root.Velocity = Vector3.new(0, 0, 0) end
    else
        -- Walkspeed Loop
        if hum.PlatformStand then hum.PlatformStand = false end
        if States.SpeedEnabled then
            local moveDirection = Vector3.new(0, 0, 0)
            if UserInputService:IsKeyDown(Enum.KeyCode.W) then moveDirection = moveDirection + Camera.CFrame.LookVector end
            if UserInputService:IsKeyDown(Enum.KeyCode.S) then moveDirection = moveDirection - Camera.CFrame.LookVector end
            if UserInputService:IsKeyDown(Enum.KeyCode.A) then moveDirection = moveDirection - Camera.CFrame.RightVector end
            if UserInputService:IsKeyDown(Enum.KeyCode.D) then moveDirection = moveDirection + Camera.CFrame.RightVector end
            moveDirection = Vector3.new(moveDirection.X, 0, moveDirection.Z)
            if moveDirection.Magnitude > 0 then root.CFrame = root.CFrame + (moveDirection.Unit * States.SpeedValue * deltaTime) end
        end
    end
end)
